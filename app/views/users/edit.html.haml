- content_for :subtitle do
  Edit User
  
#section1
  = error_messages_for :user
  - form_for(:user, :url => user_path(@user), :html => { :method => :put }) do |f|
    = render :partial => "form", :locals => { :f => f } 

    %p
    = submit_tag "Update"

#section2
  = link_to('Show', user_path(@user)) + ' | ' + link_to('List Users', users_path)
